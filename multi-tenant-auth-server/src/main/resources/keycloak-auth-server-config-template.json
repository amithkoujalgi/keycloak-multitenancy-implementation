{
  "realm": "{realm-name}",
  "resource": "{realm-name}-auth",
  "auth-server-url": "http://{realm-name}.localhost:{kc-auth-port}/auth",
  "ssl-required": "external",
  "credentials": {
    "secret": "password"
  }
}